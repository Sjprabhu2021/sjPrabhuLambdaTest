{
  "name": "lambda-playwright-cert",
  "version": "1.0.0",
  "scripts": {
    "tunnel": "node startTunnel.js",
    "test": "npx playwright test",
    "test:lambda": "npx playwright test --config=lambdatest.config.ts"
    
  },
  "devDependencies": {
    "@playwright/test": "^1.43.1",
    "dotenv": "^16.3.1",
    "@lambdatest/node-tunnel": "^1.4.4"
  }
}
